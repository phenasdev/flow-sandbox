{
  "name": "flow-sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test bed for /flow. The loop is under test here, not the code.",
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
